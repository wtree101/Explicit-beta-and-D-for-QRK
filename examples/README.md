# Examples

The supported numerical examples live in `qrk_analysis/programs/`. The
top-level heatmap drivers remain in place for command compatibility while the
implementation lives in `experiments/heatmaps/`.
