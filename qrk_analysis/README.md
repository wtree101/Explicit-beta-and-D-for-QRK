# `qrk_analysis`

This is the canonical theory package. Use `qrk_analysis.upper_bound.smallest_D`
for the required integer subsample size and `qrk_analysis.feasibility.check`
for the Massart, fixed-supremum, and Gaussian-supremum feasibility conditions.

The `qrk_adv` package at the repository root is compatibility-only.
